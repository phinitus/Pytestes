phora = float(input("Digite o valor da hora trabalhada: "))
horas_trabalhadas = float(input("Digite a quantidade de horas trabalhadas no mês: "))

salario = phora * horas_trabalhadas

print(f"O seu salário este mês é: {int(salario)}")